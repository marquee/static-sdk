
parses the selectors in /assets/style.css and generates the
elementQuery data structure, then includes the necessary JS

requires fork of elementQuery that splits the two parts up in
a requirable way

module.exports = React.createClass
    displayName: 'ElementQuery'
    render: ->
        <script>

        </script>